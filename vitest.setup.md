# vitest.setup.ts

## Purpose

Documents vitest.setup.ts, a source/config file in this project. It defines ResizeObserverMock, PointerEvent.

## Behavior

Provides maintained project behavior. Keep callers, tests, and configuration references synchronized when editing it.

## Maintenance

Update this companion when vitest.setup.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
