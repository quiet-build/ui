# deploy-storybook.yml

## Purpose

Documents .github/workflows/deploy-storybook.yml, a CI workflow in this project.

## Behavior

Runs automation for this repository. Keep triggers, permissions, secrets, and deployment targets aligned with the platform it protects.

## Maintenance

Update this companion when .github/workflows/deploy-storybook.yml changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
