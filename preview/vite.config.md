# vite.config.ts

## Purpose

Documents preview/vite.config.ts, a tooling/configuration in this project.

## Behavior

Configures local tooling or deployment. Keep paths, aliases, generated outputs, and runtime bindings synchronized with the files they reference.

## Maintenance

Update this companion when preview/vite.config.ts changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
