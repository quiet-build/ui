# tsconfig.json

## Purpose

Documents tsconfig.json, a tooling/configuration in this project.

## Behavior

Configures local tooling or deployment. Keep paths, aliases, generated outputs, and runtime bindings synchronized with the files they reference.

## Maintenance

Update this companion when tsconfig.json changes in a way that affects public behavior, configuration, dependencies, data shape, or maintenance expectations.
